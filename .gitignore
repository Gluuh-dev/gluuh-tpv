# Dependencias
node_modules/
.pnpm-store/

# Builds
dist/
dist-instalador/
build/
.next/
out/
*.tsbuildinfo

# Cloudflare / OpenNext
.open-next/
.wrangler/
cloudflare-env.d.ts
.dev.vars

# Tauri / Rust
apps/desktop/src-tauri/target/

# Expo / React Native
.expo/
*.apk
*.ipa

# Entorno y secretos
.env
.env.*
!.env.example
*.pem
*.p12
*.pfx

# Notas locales con credenciales de prueba (código de instalación, contraseñas). El repo es
# PÚBLICO: nada de esto se sube. Se abre en local y ya.
*.local.md

# Logs / cachés
*.log
.turbo/
coverage/

# Sistema operativo / editores
.DS_Store
Thumbs.db
.idea/

# Supabase CLI (temporales)
supabase/.temp/

# Estado local de agentes Claude (worktrees, etc.)
.claude/

# Nodo local (spike): binarios portables y datos de Postgres. NUNCA a git.
# Todo esto es borrable: `rm -rf .nodo` y es como si no hubiera pasado.
.nodo/

# El paquete del instalador. Se monta en C:\gluuh-paquete (FUERA del repositorio), pero por
# si alguien lo apunta aquí: son 510 MB de binarios, y el .iss empaqueta `supabase\*` entero
# y recursivo — o sea que una carga aquí dentro se METERÍA A SÍ MISMA EN EL PAQUETE.
supabase/nodo/instalador/carga/
supabase/nodo/instalador/dist/
