8d7
< 	"syscall"
11,12d9
< 	"golang.org/x/sys/unix"
< 
186,196c183,185
< 	lc := net.ListenConfig{
< 		Control: func(network, address string, c syscall.RawConn) error {
< 			var serr error
< 			if err := c.Control(func(fd uintptr) {
< 				serr = unix.SetsockoptInt(int(fd), unix.SOL_SOCKET, unix.SO_REUSEPORT, 1) // #nosec G115
< 			}); err != nil {
< 				return err
< 			}
< 			return serr
< 		},
< 	}
---
> 	// setReusePort es específica de plataforma (listen_unix.go / listen_windows.go):
> 	// en Unix activa SO_REUSEPORT; en Windows, que no la tiene, no hace nada.
> 	lc := net.ListenConfig{Control: setReusePort}
