# Traspaso de sesión — 07/07/2026

Continuación del 06/07 (ver `2026-07-06-traspaso.md`). Frente: **backoffice estilo
Ágora** (tabla reutilizable), **app de escritorio** y **plan de acceso/seguridad**.

## Hecho (aplicado y verificado)

### Tabla reutilizable `TablaDatos` (componente clave del panel)
- Alto completo, cebra, cabecera/footer en color distinto, **columnas fijas**
  (check·#·Nombre) al scroll lateral, **divisores verticales**, ordenar por
  cabecera (chevron oculto hasta hover; resaltado si ordena).
- **Barra Nuevo/Duplicar/Editar/Eliminar + buscador integrado + slot de filtros**
  (botones rellenos: secondary/destructive). Familias, categorías, grupos
  mayores, productos y **series** cableadas. **Duplicar** copia con id nuevo y
  « - copia» (productos también copia categorías m2m, formatos y modificadores;
  PLU vacío). Productos: columna Cód. barras + filtro de categoría en la barra.

### Notificaciones (Sileo)
- Migradas de sonner a **Sileo** (`app/lib/toast.ts`, adaptador con la API de
  sonner). Posición: **abajo centrado**. Título corto + detalle en el contenido.

### App de escritorio (Electron)
- **Instalador de Windows generado**: `pnpm --filter @gluuh/desktop dist` →
  `apps/desktop/dist-instalador/Gluuh TPV Setup 0.1.0.exe` (~98 MB, NSIS).
  `dist-instalador/` ignorado en git. **Sin firma** (SmartScreen avisará).
- **IP/URL del servidor configurable**: el desktop lee `config.json`→`servidor`
  en runtime (precedencia config > GLUUH_URL > localhost), sin re-empaquetar.
- **Editable desde Configuración**: puente `window.gluuh.leerConfigTerminal/
  guardarConfigTerminal` (preload + IPC + `guardarConfig` escritor); componente
  `components/config-terminal.tsx` en la config de impresión (IP servidor +
  carpeta backup). Al cambiar el servidor, la app recarga sola. En navegador,
  avisa que solo se configura desde el escritorio.

### Empresa y Local (S1 de la guía 13)
- **Migración 0069 (aplicada por MCP)**: `location` gana nombre_comercial,
  poblacion, provincia, codigo_postal, contacto, telefono, email, web.
- `/ajustes` rehecho como **ficha profesional a dos columnas** (Datos
  administrativos · Contacto · Ubicación · Fiscalidad).

### Documentación (análisis, sin implementar)
- **Guía 13** (`docs/implementacion/13-…`): backlog de rediseño de config +
  estilos (footer fijo E1, buscador global E2, TanStack Query E3, Seguridad,
  marca, orden botones, arreglar navegación, imagen en ticket S6).
- **Guía 14** (`docs/implementacion/14-…`): **modelo de identidad/acceso**
  decidido con el cliente — activación por **licencia del tenant** (una vez),
  **operarios con código+clave** (sin email) y **perfiles** que deciden accesos,
  **superficies por puerta** (TPV/Config/Móvil), email solo dueño/técnico remoto,
  **usuarios por defecto** (técnico 1212, admin 1111), **asistente de primer
  arranque** (aceptar + IP servidor + directorio backup + código tenant + crear
  usuario), zona técnica→Seguridad, cerrar día, audit_log.

### Buscador global E2 (implementado)
- `components/command-palette.tsx`: `Ctrl/⌘+K`, busca páginas del menú +
  productos/familias/categorías; **alto fijo 498px** (escritorio) / 70vh (móvil).
  Backlog v2 (buscar en configuración + sesiones) apuntado en guía 13.

### Seguridad y perfiles "de verdad" (implementado)
- **`/seguridad`** (Administración→Usuarios): bloqueo del TPV, clave técnica y
  passkey (movidos desde `/ajustes`).
- **`/plantillas-ticket`** con página propia (fin del redirect): diseño del ticket
  + vista previa; impresoras/rutas quedan en `/configuracion-de-impresion`.
- **Perfiles estilo Ágora (permiso = aplicación × acción)**:
  - Migración **0070** (MCP): `app_user.perfil_id` (FK, `on delete set null`).
  - Catálogo en `app/lib/permisos.ts`: Punto de venta (5) + Zonas del panel (5) +
    Áreas sensibles (`admin.usuarios/catalogo/fiscal/tecnica`).
  - `/perfiles`: matriz agrupada + buscador + copiar + **perfiles recomendados**
    (Administrador/Encargado/Camarero/Informes).
  - `/empleados`: el usuario **solo elige perfil**; permisos **en vivo** (espejo del
    perfil — editarlo afecta a la vez a todos sus usuarios).
  - **Bloqueo real**: el menú oculta lo no permitido **y un guardián de ruta corta el
    acceso directo por URL** («Sin acceso»); PROPIETARIO nunca se bloquea. Verificado
    con self-checks (8 + 11 asserts).
  - **Bloqueo real**: menú + guardián de ruta (URL). **Perfiles recomendados**
    sembrados al crear empresa. **Permisos de acción del TPV** (aparcar, agotado,
    crear_producto, abrir_cajon) en cliente.
  - **Respaldo RLS** (defensa en profundidad): `operario_permite()` + políticas
    RESTRICTIVE — escritura de `perfil`/`app_user` (`0071`, bloquea autoescalada) y
    de catálogo `family`/`category`/`modifier_group` (`0072`). Verificado por
    impersonación. `tax_rate` ya cerrado (RLS sin políticas); `product` gateado en
    cliente (lo escribe el TPV).
  - ⚠️ **Bug arreglado**: `app_user.permisos` (0041) **nunca se aplicó** → las
    queries fallaban en silencio. Los permisos viven **solo en `perfil.permisos`**;
    el usuario los hereda por `perfil_id`. Quitado de layout/TPV/empleados.
- **Guía 14 reescrita** como documento maestro (Parte A: cómo funciona hoy,
  verificado en código; Parte B: decisiones + plan + huecos).

## Pendiente (en orden)
1. **RLS de permisos sensibles** (respaldo servidor del gating de perfil; hoy es
   cliente) — guía 14 B.7. Tarea deliberada, tabla por tabla, con pruebas.
2. Guía 14 SA2 restante: **login local código+clave** + código de operario legible.
3. Ampliar catálogo de permisos (Caja, Almacén, Informes granular) y afinar más
   páginas con su `perm`.
4. Guía 13: **footer fijo E1**; terminar S1 (sacar marca/orden de `/ajustes`),
   S5 (arreglar nav). Buscador v2 (config + sesiones).
5. Guía 14: **cerrar día / caja por terminal** (SA4) y **audit_log**.
6. Copia en la nube de la config del terminal (`setting` DEVICE) — nota en guía 13.

## Datos operativos
- Migración BD **por MCP** (token personal ya configurado); última = **0072**
  (0070 `perfil_id`, 0071 RLS perfil/app_user, 0072 RLS catálogo).
  Espejo en `apps/api/db/schema.sql`. VERIFACTU sigue en pruebas (decisión: lo
  último antes de vender).
