# Traspaso de sesión — 17/07/2026

Auditoría técnica integral del monorepo, Supabase por migraciones/tipos, nodo local, seguridad, fiscalidad, rendimiento y calidad. Fue diagnóstico en solo lectura: no se modificó código ni se ejecutó SQL.

## 1. Decisiones

- Mantener nodo/PostgreSQL como arquitectura offline v1; `packages/sync`/PowerSync queda experimental hasta una decisión explícita.
- Mantener shared schema multi-tenant, reforzado con autorización fail-closed, RPC tenant-scoped y FK compuestas en agregados críticos.
- No activar VERIFACTU ni AEAT hasta disponer de transacción/idempotencia/outbox y corregir el camino F1.
- La implementación debe empezar por seguridad/aislamiento, no por dividir los componentes grandes.

Documento vigente: [`docs/auditoria/README.md`](../auditoria/README.md).

## 2. Hecho y verificado

- Auditoría y roadmap creados en `docs/auditoria/`.
- `corepack pnpm typecheck`: 12/12 tareas; `ui` y `api-client` siguen con placeholder.
- `corepack pnpm test`: 91 tests verdes (44 core, 10 API, 37 web).
- `corepack pnpm lint`: falla con 4 errores y 43 avisos; los tipos UTF-16 se detectan como binario.
- Tipos generados: 82 tablas y 43 funciones `public`, PostgREST 14.5; no están conectados a los clientes.
- Supabase cloud: **no consultado**. El MCP está configurado para `gxcqihslbicrszgzudjs`, pero sus herramientas no estaban cargadas en esta tarea.
- Migraciones: ninguna aplicada ni reservada. El siguiente número sigue siendo `0111` mientras `AHORA.md` no diga lo contrario.

## 3. Pendiente, en orden

1. Verificar por MCP read-only si `0105` está ausente en cloud y revisar grants/owners/RLS efectivos.
2. Autorizar o no el Lote S1: jornada/heartbeat tenant-scoped y suite adversarial.
3. Lote S2: permisos fail-closed con preflight/backfill.
4. Lote S3: media y diagnóstico/acciones del nodo.
5. Diseñar y caracterizar el write-path transaccional/idempotente de venta/factura.

## 4. Datos operativos / gotchas

- `0105` existe en repo y sus RPC se invocan, pero no aparece en los tipos recién generados; `0106`–`0110` sí. Es una inferencia de drift, no una comprobación viva.
- `jornada_abierta`, `cerrar_jornada` y `device_heartbeat` tienen riesgos demostrados de autorización en sus definiciones SQL.
- No versionar todavía `supabase/types/database.types.ts` tal como está: es UTF-16 LE y rompe lint. Regenerarlo/converterlo a UTF-8 mediante comando reproducible.
- No mezclar el commit de auditoría con los cambios TPV que ya estaban en el working tree.
