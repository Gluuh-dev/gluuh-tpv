# Traspaso de sesión — 17/07/2026

Auditoría técnica integral del monorepo, Supabase por migraciones/tipos, nodo local, seguridad, fiscalidad, rendimiento y calidad. La primera parte fue diagnóstico en solo lectura. Tras aprobarse el plan definitivo comenzó la Fase 0, sin ejecutar SQL ni cambiar Supabase.

## 1. Decisiones

- Mantener nodo/PostgreSQL como arquitectura offline v1; `packages/sync`/PowerSync queda experimental hasta una decisión explícita.
- Mantener shared schema multi-tenant, reforzado con autorización fail-closed, RPC tenant-scoped y FK compuestas en agregados críticos.
- No activar VERIFACTU ni AEAT hasta disponer de transacción/idempotencia/outbox y corregir el camino F1.
- La implementación debe empezar por seguridad/aislamiento, no por dividir los componentes grandes.

Documento vigente: [`docs/auditoria/README.md`](../auditoria/README.md).

## 2. Hecho y verificado

- Auditoría y roadmap creados en `docs/auditoria/`.
- `corepack pnpm typecheck`: 12/12 tareas; `ui` y `api-client` siguen con placeholder.
- `corepack pnpm test`: 91 tests verdes (44 core, 10 API, 37 web).
- `corepack pnpm lint`: falla con 4 errores y 43 avisos; los tipos UTF-16 se detectan como binario.
- Tipos generados: 82 tablas y 43 funciones `public`, PostgREST 14.5; no están conectados a los clientes.
- Supabase cloud: **no consultado**. El MCP está configurado para `gxcqihslbicrszgzudjs`, pero sus herramientas no estaban cargadas en esta tarea.
- Migraciones: ninguna aplicada ni reservada. El siguiente número sigue siendo `0111` mientras `AHORA.md` no diga lo contrario.

## 3. Pendiente, en orden

1. Verificar por MCP read-only si `0105` está ausente en cloud y revisar grants/owners/RLS efectivos.
2. Autorizar o no el Lote S1: jornada/heartbeat tenant-scoped y suite adversarial.
3. Lote S2: permisos fail-closed con preflight/backfill.
4. Lote S3: media y diagnóstico/acciones del nodo.
5. Diseñar y caracterizar el write-path transaccional/idempotente de venta/factura.

## 4. Datos operativos / gotchas

- `0105` existe en repo y sus RPC se invocan, pero no aparece en los tipos recién generados; `0106`–`0110` sí. Es una inferencia de drift, no una comprobación viva.
- `jornada_abierta`, `cerrar_jornada` y `device_heartbeat` tienen riesgos demostrados de autorización en sus definiciones SQL.
- No versionar todavía `supabase/types/database.types.ts` tal como está: es UTF-16 LE y rompe lint. Regenerarlo/converterlo a UTF-8 mediante comando reproducible.
- No mezclar el commit de auditoría con los cambios TPV que ya estaban en el working tree.

## 5. Plan aprobado y comienzo de Fase 0

- Plan canónico guardado en [`docs/plan/14-plan-definitivo-reparacion-identidad-seguridad.md`](../plan/14-plan-definitivo-reparacion-identidad-seguridad.md).
- Alcance de la primera entrega: Fase 0 + diseño SQL revisable de Fase 1; ninguna migración remota.
- `database.types.ts` convertido mecánicamente de UTF-16 LE a UTF-8 sin BOM.
- Clientes principales conectados a un contrato transitorio derivado de `Database`: valida tablas/RPC y deja el CRUD histórico abierto hasta migrarlo a tipado estricto.
- Retirado del camino activo el diseño rechazado de `0105`: panel y Electron vuelven a emparejado efímero de seis dígitos; `/auth/v1/dispositivo` falla cerrado con `410`.
- Supabase CLI fijado en el workspace para preparar generación reproducible. El gate y el documento SQL de Fase 1 siguen pendientes.
- Verificación parcial: typecheck individual verde en `packages/supabase`, web, móvil y desktop.
- No se creó ni reservó `0111`; no se ejecutó SQL local ni remoto.

## 6. Plan maestro de implementación F0–F8

- Creada la guía canónica
  [`docs/implementacion/19-plan-maestro-reparacion-f0-f8.md`](../implementacion/19-plan-maestro-reparacion-f0-f8.md),
  que convierte el plan estratégico en 34 entregas ordenadas y verificables.
- Cada fase incluye prioridad, módulos afectados, migraciones lógicas propuestas,
  dependencias, criterios de aceptación, pruebas, riesgos, rollout y condiciones de STOP.
- Se reconciliaron los planes técnicos `plans/016–026` dentro de F0, F1, F5, F6,
  F7 y F8 para evitar duplicar trabajos o ejecutarlos fuera de orden.
- Las guías 14 y 15 quedaron marcadas como históricas/contextuales donde contradicen
  el modelo definitivo; la guía 16 queda subordinada al nuevo orden y excluye LWW en
  identidad, permisos, dinero y fiscalidad.
- El índice de implementación ya no presenta `apps/api/db/schema.sql` como espejo:
  se conserva únicamente como documentación histórica.
- Este bloque solo cambia documentación. No creó migraciones, no ejecutó SQL y no
  modificó Supabase ni el Postgres local.

## 7. Sesión chat (Claude) — F0 completada hasta la puerta de aprobación

Continuación directa del §5 sobre el mismo árbol. **F0 queda entregada**; lo único
no ejecutado es el reset del harness (requiere nodo desechable). Verificación
global: typecheck 12/12 · tests 91/91 · `contrato:check` verde (666 usos).

- **Drift confirmado por MCP (solo lectura)** y consolidado en
  [`docs/auditoria/08-baseline-esquema-2026-07-17.md`](../auditoria/08-baseline-esquema-2026-07-17.md):
  `0105` **NO está en la nube** (la nota de `AHORA.md` era falsa); `0106/0107`
  aplicadas fuera de banda; `0108–0110` al día; historial remoto 35/110.
- **Hallazgo nuevo**: `admin_sembrar_terminal_defecto` viva en la nube pero ROTA
  (referencia `device.usuario` y `fijar_clave_dispositivo`, inexistentes) —
  fallaba SIEMPRE y `crear-empresa` descartaba el error: **ninguna empresa creada
  desde la nube ha tenido jamás terminal por defecto**. Llamada muerta eliminada
  de `crear-empresa/route.ts` (comportamiento idéntico); la función se retira de
  la nube en F4 con migración idempotente.
- **Gates nuevos** (documentados en `supabase/README.md`):
  `pnpm tipos:generar` / `tipos:check` (`scripts/generar-tipos-supabase.mjs`:
  UTF-8 sin BOM, LF, reemplazo atómico, drift contra la nube) y
  `pnpm contrato:check` (`scripts/verificar-contrato-supabase.mjs`: todo
  `.from()`/`.rpc()` literal debe existir en el contrato — impide que `0105`
  reaparezca). README de supabase corregido (ya no dice que `schema.sql` es espejo).
- **Harness 0.2**: `scripts/verificar-migraciones-nodo.mjs` (solo 127.0.0.1:55432/gluuh,
  exige `GLUUH_DB_DESECHABLE=1`, aplica 0001–0110 desde cero, fixtures A/B,
  contrasta con el contrato). **Implementado, NO ejecutado**: no hay nodo
  desechable autorizado; queda bloqueado tal como prescribe la guía 19 §6.2.
- **Diseño SQL de F1 escrito**:
  [`docs/implementacion/20-diseno-sql-identidad-global.md`](../implementacion/20-diseno-sql-identidad-global.md)
  — DDL (cuenta, sesion_contexto, app_user_local, overrides, sesiones/eventos),
  preflight ya ejecutado (**0 duplicados**; los 4 `app_user` sin perfil → el
  backfill de perfiles del plan 018 es prerequisito del switch), funciones v2
  fail-closed con las MISMAS firmas, grants, RLS, matriz de 13 casos,
  expand/backfill/switch/contract, rollback y pruebas que deben fallar antes.
- **Seguimiento vivo del programa**: `docs/estado/REPARACION-F0-F8.md`.

**SIGUIENTE PASO = PUERTA 1 (humana):** revisar y aprobar el documento 20.
Sin esa aprobación no se reserva `0111` ni se escribe ninguna migración.

## 8. Sesión chat (Claude) — F1 y F2 escritos hasta la puerta de aplicación

Puerta 1 aprobada por el usuario («sigue con el f1 y f2»). Todo escrito y
verificado en el árbol (typecheck 12/12 · tests 91/91 · `contrato:check` 679 usos);
**NADA aplicado en Supabase ni en el nodo** — la puerta 4 sigue cerrada.

**SQL (0111–0115, reservadas en `AHORA.md`, siguiente libre 0116):**
- `0111_identidad_global_expand` — cuenta global 1:1 con auth, contexto por
  `session_id`, asignación por local, overrides PERMITIR/DENEGAR, registro de
  sesiones y eventos; RLS `force` y sin grants a anon desde el nacimiento.
- `0112_identidad_global_backfill` — cuentas + enlaces + asignaciones; **materializa
  los `true` implícitos** de todos los perfiles (21 claves del catálogo) y asigna
  perfil a los 4 usuarios sin él (ENCARGADO→Encargado, resto→Camarero); aborta con
  conteo si queda algo sin migrar.
- `0113_identidad_fail_closed` — `current_tenant_id()` v2 (GUC nodo → contexto de
  sesión → claim transicional → membresía única → NULL; adiós LIMIT 1),
  `operario_permite()` v2 (ausente=denegado; override DENEGAR gana),
  `establecer_contexto_sesion`, `mis_membresias`, `revocar_sesion`; revokes.
- `0114_endurecer_rpc_privilegiadas` — jornada_abierta/cerrar_jornada/device_heartbeat
  con guardia de tenant (`_exigir_tenant`); el nodo local (sin claims JWT) sigue
  confiable; anon fuera de heartbeat; autor del cierre validado contra el tenant.
- `0115_invitaciones_y_alta_titular` — `invitacion` (solo hash SHA-256 del token,
  7 días, una pendiente por email+empresa), `tenant.estado_alta`, `canjear_invitacion`
  atómica (EMITIDA→ACEPTADA en un UPDATE: sin carrera de doble canje).

**Web:**
- `(panel)/layout.tsx` — bootstrap FAIL-CLOSED: estados cargando/error/identidad
  incompleta; sin fallback `PROPIETARIO`/`{}`; menú no se monta sin identidad.
- `lib/contexto.ts` + `/elegir-empresa` + login — contexto por sesión con
  degradación limpia mientras 0113 no esté aplicada.
- `/api/invitaciones` (emitir; requiere `operario_permite('admin.usuarios')`),
  `/api/invitaciones/canjear` (GET info + POST canje; cuenta nueva con contraseña
  propia o membresía añadida a cuenta existente), página `/invitacion/[token]`.
- `/api/cuenta/cambiar-password` — el cambio lo hace el SERVIDOR y solo entonces
  limpia `debe_cambiar_password` (antes el cliente podía autolimpiar la metadata
  sin cambiar nada); `/cambiar-password` usa la ruta.
- Transición tipada: `TablasPendientes/FuncionesPendientes` en `packages/supabase`
  + `EXCEPCIONES` en el gate — **quitar ambos espejos al aplicar la tanda y
  regenerar tipos**.

**Pruebas:** `apps/nodo/pruebas/prueba-identidad-fail-closed.mjs` (multiempresa,
tenant ambiguo→NULL, camarero sin perfil denegado, jornada/heartbeat cross-tenant).
Diseñada para FALLAR antes de 0111–0114 y pasar después. El nodo estaba apagado:
no ejecutada.

**Qué falta de F1/F2 (consciente):**
- F1 entrega 1.5 (contract: retirar unicidades globales y LIMIT 1 residual) —
  tras canary con la tanda aplicada.
- F2: MFA (2.3), revocar resto de sesiones al cambiar contraseña, caducidad/cifrado
  de la contraseña temporal del titular + PDF al vuelo (**STOP del plan: falta
  gestión segura de claves**), cuenta provisional offline (2.4), PIN del titular.

**SIGUIENTE PASO = PUERTA 4:** revisar las 5 migraciones y autorizar su aplicación
(primero en un nodo desechable con el harness, luego Supabase). Tras aplicar:
regenerar tipos, quitar los dos espejos de transición y correr la prueba adversarial.

## 9. Sesión chat (Claude) — 0111–0115 APLICADAS EN LA NUBE (puerta 4 autorizada)

El usuario autorizó aplicar la tanda por MCP («haz tu las migraciones con el mcp
supabase»). Aplicadas en orden con preflight y verificación:

- **Preflight**: 0 duplicados de auth/email; roles reales PROPIETARIO×2 (con auth)
  y CAMARERO×2 (sin auth, entran por PIN); 0 perfiles en toda la BD.
- **0111–0115 aplicadas** (`0113` necesitó un fix: `min(uuid)` no existe en
  Postgres → `(array_agg(distinct …))[1]`; corregido también en el fichero local).
- **Backfill verificado**: 2 cuentas, 0 sin enlazar, 4 perfiles creados
  (materializados con las 21 claves), 0 camareros sin perfil, 4 asignaciones.
- **Smoke fail-closed en la nube**: identidad desconocida → `current_tenant_id()`
  NULL y `operario_permite()` false; anon SIN EXECUTE en heartbeat/jornada;
  propietario conserva todo (rol explícito).
- **Advisors**: 57 avisos, todos WARN y preexistentes o intencionales (definer
  ejecutable por authenticated = el diseño); ninguna función nueva con
  search_path mutable.
- **Tipos regenerados por MCP** (89 tablas/vistas, 48 RPC) y **espejos de
  transición retirados** (EXCEPCIONES del gate + tipos pendientes de
  `packages/supabase`). Verificación: contrato 679 usos verde · typecheck 12/12 ·
  tests 91/91.

**Pendiente que abre esto:**
1. **Nodo**: al levantarlo, aplicar 0111–0115 y correr
   `prueba-identidad-fail-closed.mjs` (los camareros del PIN dependen del auth
   propio del nodo — verificar su flujo con 0113/0114 antes de dar por buena la
   tanda allí).
2. **Humo manual en la nube**: login del titular real (debe seguir entrando;
   ahora con contexto de sesión), emitir/aceptar una invitación de prueba.
3. F1 contract (1.5) tras canary; restos de F2 (MFA, revocar sesiones, temporal
   cifrada, provisional offline).

## 10. Sesión chat (Claude) — F2 2.3 + F3 3.1/3.2 («sigue con todo lo del plan»)

Verificación: contrato 686 usos verde · typecheck 12/12 · tests 91/91.

**F2 entrega 2.3 (aplicable ya, usa 0111/0113 que están en la nube):**
- `/seguridad` gana dos tarjetas: **Sesiones de tu cuenta** (lista de
  `sesion_registro` con RLS de cuenta + revocación individual vía `revocar_sesion`;
  la sesión actual se marca y no se puede revocar a sí misma) y **Verificación en
  dos pasos** (TOTP nativo de Supabase Auth: enroll con QR/clave manual,
  verify, unenroll — compatible offline en el móvil). Componente:
  `apps/web/components/seguridad-cuenta.tsx`.
- Cambiar la contraseña ahora **revoca todas las demás sesiones**
  (`auth.signOut({ scope: "others" })` tras el cambio server-side).

**F3 entregas 3.1/3.2 (núcleo):**
- **`0116_orden_instalacion_y_nodo` ESCRITA pero SIN aplicar**: el apply por MCP
  lo bloqueó el clasificador de permisos del asistente (las 0111–0115 sí pasaron).
  Reintentar/aplicar y luego: regenerar tipos + quitar las excepciones `0116` del
  gate y el bloque `Pendientes0116*` de `packages/supabase`.
- Contenido de 0116: `orden_instalacion` (hash sha256 del código — el claro viaja
  una vez —, caducidad 30 días, reserva 24 h ligada a un intento, un solo uso,
  revocación) + `nodo_instancia` (clave pública/fingerprint/versión/estado/
  revocación/reemplaza_a) + `canjear_orden_instalacion` (máquina de estados
  completa y atómica, con reanudación del mismo intento y expropiación de
  reservas caducadas).
- `/api/admin/orden-instalacion` (POST): emite órdenes — solo `es_admin_plataforma`
  desde admin.gluuh.com; el código se devuelve UNA vez.
- `/api/instalacion/activar`: prueba primero la ORDEN (canje atómico; errores
  distinguidos 410 caducado / 409 reservado por otro) y cae al flujo legacy
  `tenant.codigo_instalacion` — los instaladores existentes no se rompen.

**Pendiente inmediato:** aplicar 0116 · humo manual (login titular, invitación,
/seguridad) · nodo con 0111–0115 + prueba adversarial cuando se levante.
**Siguiente del plan:** F3.3 (licencia/entitlements — necesita decisiones
comerciales, puerta 5), F3.4 (instalador W11), F4 (emparejado v2 y PIN).

## 11. Sesión chat (Claude) — 0116 aplicada (por el usuario) y verificada en vivo

- El usuario ejecutó `0116` a mano en Supabase. Verificado por MCP: 2 tablas +
  RPC presentes, anon sin EXECUTE.
- **Humo real de la máquina de estados**: orden de prueba → primer canje `OK`
  (crea `nodo_instancia`, resuelve empresa/local), segundo canje `INVALIDA`
  (un solo uso). Datos de humo eliminados (0 órdenes, 0 nodos).
- Tipos regenerados por MCP (91 tablas/vistas, 49 RPC) y espejos de transición
  de 0116 retirados (gate + `packages/supabase`).
- Verificación: contrato 686 usos verde · typecheck 12/12 · tests 91/91.

**Todo lo escrito está ahora aplicado en la nube (0111–0116).** Siguiente del
plan: F3.3 licencia/entitlements (puerta 5: decisiones comerciales del
propietario), F3.4 instalador W11 con claves de nodo, F4 emparejado v2 + PIN.
Recordatorios vivos: nodo local sin 0111–0116; humo manual del titular/invitación/
/seguridad; push.

## 12. Sesión chat (Claude) — F4 núcleo aplicado («sigue con el f3 y el f4»)

F3.3 (licencia) queda a la espera de la puerta 5 (reglas comerciales del
propietario). F4 ejecutado en su núcleo. Verificación: contrato 691 usos ·
typecheck 12/12 · tests 91/91 · humo de rotación en vivo.

**0117 `emparejado_v2_y_operario` — APLICADA en la nube por MCP:**
- `credencial_dispositivo`: refresh ROTATORIO con hash sha256 server-side,
  expiración 90 días, revocación, versión. RPCs service-only
  `emitir_credencial_dispositivo` / `renovar_credencial_dispositivo` (rotación
  atómica: el hash viejo se consume — humo verificado: reuso = 0 filas).
- `sesion_operario` (una por terminal) y `pin_intento`: el backoff del PIN pasa a
  ser POR TERMINAL (`validar_pin_terminal(p_pin, p_device)`); la firma histórica
  `validar_pin(text)` delega en el canal "sin terminal". **Adopción pendiente:
  el TPV debe pasar su device_id** (no toqué `tpv/page.tsx`: sucio de otra sesión).
- 4.4 parcial: `drop function admin_sembrar_terminal_defecto` (estaba rota) y
  `crear-empresa` ya no siembra `tecnico/1212`, `admin/1111`, camareros de ejemplo.

**Rutas:**
- `canjear`: además del JWT legacy (acortado 365d→30d), emite `access` (12 h,
  jti) + `refresh` rotatorio. Los clientes viejos ignoran los campos nuevos.
- `renovar` (nueva): rota el refresh y emite access; credencial revocada → 401
  y a re-emparejar. `revocar` (nueva): admin del tenant corta el terminal
  (revoca credenciales + cierra su sesión de operario) sin afectar a los demás.
- **Techo conocido**: el JWT legacy de 30 días NO es revocable; su retirada
  (F4.4 final) = todos los clientes en v2 + rotar `DEVICE_JWT_SECRET`.

**Además**: `PERFILES_RECOMENDADOS` materializados en `permisos.ts` — sin esto,
una empresa NUEVA sembraría `Administrador = {}` que con el fail-closed de 0113
significaría «nada permitido». Tipos regenerados (94 tablas / 51 RPC; la función
rota ya no aparece).

**Pendiente F4**: 4.2 safeStorage/IPC (BLOQUEADA: `apps/desktop/src/*` y
`conectar-terminal.html` sucios por otra sesión — coordinarse), adopción cliente
de access/refresh y del PIN por terminal, retirada 0105/0107 en nodos (cuando se
levanten), PIN temporal visible una vez en el alta de empleados.
**Siguiente del plan**: F3.3 licencia (puerta 5), F3.4 instalador, F5 (LAN/nodo:
plans/023 — el nodo está apagado; se puede escribir el código igualmente).

## 13. Sesión chat (Claude) — F5 núcleo + F6 fiscal («sigue con el f5 y el f6»)

Verificación: contrato 691 usos (95 tablas/52 RPC) · typecheck 12/12 · tests 91/91.

**F5 (plans/023) — superficie LAN del nodo, en código (nodo apagado: probar al
levantarlo con `prueba-superficie-lan.mjs`):**
- `gateway.mjs`: nuevo `/nodo/health` público MÍNIMO (vivo+versión); `/nodo/estado`
  detallado deja de ser anónimo (solo consola local o token firmado por el bar —
  `verificar()` nuevo en `secreto.mjs`); acciones solo POST + Origin propio.
  ⚠ Si el panel de escritorio llamaba a las acciones por GET, debe pasar a POST
  (los ficheros desktop están en la otra sesión).
- `media.mjs`: subir exige token del bar (el anon key del nodo vale — supabase-js
  ya lo manda; cero cambios de cliente), tope 15 MB cortando el stream, solo
  extensiones de carta, escritura temporal+rename. Servir (GET) sigue público.
- `descargar-imagenes.mjs`: SOLO descarga del origen del Supabase del bar
  (`redirect: "error"`), contención de ruta bajo media/, timeout 30 s y tope
  20 MB/fichero. El descubrimiento por columnas se mantiene: con la allowlist de
  origen, una URL colada en un campo de texto ya no puede apuntar a nada interno.

**F6 (plans/020) — emisión fiscal atómica (0111–0118 todas en la nube):**
- **0118 aplicada por MCP** y probada en vivo (serie ZZ-TEST, borrada): emisión
  OK deja factura + desglose JUNTOS; mismo número concurrente → COLISION sin
  rastro. `fiscal_outbox` con PENDIENTE/ENVIANDO/ACEPTADA/RECHAZADA/REINTENTABLE
  y lease. Unicidad `(tenant, order_id)` (0 facturas en nube: sin preflight).
- `/api/factura`: **bug de cadena arreglado** — la huella se calculaba SIEMPRE
  con `tipoFactura: "F2"` aunque se persistiera F1; ahora huella, XML y fila
  cuentan lo mismo. La emisión pasa por la RPC (una tentativa = una transacción);
  `EXISTE` → devuelve la factura previa con `yaEmitida: true` (el reintento tras
  timeout no consume número ni huella). Encolado AEAT tras `VERIFACTU_ENVIO=1`
  (apagado; no hay worker todavía).

**NO hecho y por qué:** 6.1/6.2 venta atómica server-authoritative — necesita la
puerta 6 (política de propina/redondeo/pago parcial, decisión del propietario) y
toca `tpv/page.tsx` (sucio de otra sesión). 5.3 (soporte/break-glass/backups) y
el worker AEAT + guards de `apps/api` quedan como siguiente bloque.

## 14. Sesión chat (Claude) — F6 completado hasta donde los callers permiten

Verificación: contrato 691 usos (95 tablas/55 RPC) · typecheck 12/12 · tests
91/91 · dos humos SQL en vivo.

**0119 `cobro_atomico_y_outbox_worker` — APLICADA por MCP:**
- `cobrar_cuenta(order, pagos, operario, client_id)`: candado FOR UPDATE +
  validación de la SUMA contra el total de la BD + inserción de pagos + COBRADA,
  todo en una transacción. Humo en vivo: suma-mal → SUMA_NO_CUADRA; pago mixto
  EFECTIVO+TARJETA con propina → OK; cobro desde "otro terminal" → YA_COBRADA;
  reintento con el MISMO client_id → OK sin duplicar pagos (el client_id del
  cobro se ancla al primer pago — el humo cazó que antes no se guardaba).
  Detalle fino: `operario_permite('cobrar')` solo se exige a sesiones de USUARIO
  (el nodo sin JWT y service_role pasan — el fail-closed los denegaría siempre).
- `outbox_tomar` (lease con `for update skip locked`, reintomable si el worker
  muere) y `outbox_resolver` (ACEPTADA/RECHAZADA/REINTENTABLE), service-only.

**Worker AEAT (`apps/api/src/fiscal/outbox.worker.ts`):**
- Apagado por defecto (`OUTBOX_AEAT=1` para encender). Habla con PostgREST por
  fetch + service key (cero dependencias nuevas).
- Reconstruye el DTO desde el SNAPSHOT (tax lines de 0118 + territorio del local
  + registro anterior por huella) y **verifica que la huella recalculada == la
  almacenada antes de enviar**; si no cuadra → REINTENTABLE con aviso (nunca se
  declara una cadena que no se puede reproducir). ACEPTADA solo con acuse
  (`200` + "Correcto"); 5xx/red/sin-certificado → REINTENTABLE; rechazo
  funcional → RECHAZADA para revisión humana.
- `PreviewTicketDto` gana `tipoFactura` opcional: el servicio de la API también
  hardcodeaba "F2" en huella y XML (mismo bug que /api/factura).

**Qué falta de F6 (y de quién depende):**
- Adopción: el TPV debe llamar a `cobrar_cuenta` (tpv/page.tsx, otra sesión).
- 6.2 revocar INSERT/UPDATE directos de sales_order/order_line/payment: SOLO
  tras migrar TODOS los callers (TPV, kiosko, comandera, nodo).
- Autoridad de PRECIOS server-side (comando de venta completo, plan 019 paso 1):
  necesita la puerta 6 (propina/redondeo/pago parcial) — decisión del propietario.
- Entorno AEAT de pruebas con certificado segregado para el primer envío real.

## 15. Sesión chat (Claude) — F7 núcleo (sync durable) + F8.1 parcial

Verificación: typecheck 12/12 · tests 91/91 · contrato 691 usos ·
`prueba-cursores.mjs` (pura, sin nodo) TODO VERDE.

**F7.1 — el ACK falso está muerto:** `/sync/upload` (apps/api) devolvía
`estado: "ok"` sin persistir NADA — un conector que confiara en él vaciaba su
cola y las operaciones del bar desaparecían. Ahora responde **501** con mensaje
claro; el conector PowerSync ya lanzaba ante !ok, así que conserva la cola.

**F7.2 — cursores compuestos en `sincronizar.mjs`** (¡tocado con TRAMPAS §1 y §8
delante!):
- **Foto paginada hasta agotar** (orden por PK, páginas de 1000, cinturón
  FOTO_MAX=200k): muere el `TOPE=5000` — una carta grande ya no viene cortada
  ni hace parecer borrado lo que no cupo. `propagarBorrados` conserva sus tres
  cerrojos (el 1º ahora referencia el cinturón).
- **Delta de bajada por cursor (updated_at, PK)** con `or=(gt,and(eq,pk.gt))`
  de PostgREST, paginado hasta agotar y **checkpoint por página** (aplicar →
  anotar): un corte a mitad reanuda reaplicando como mucho una página (upserts
  idempotentes), jamás saltándose filas. La subida local usa el mismo desempate
  `(updated_at, PK) > (t, k)` en SQL — un import con todas las filas al mismo
  microsegundo ya no queda a medias.
- Marcas: ahora JSON `{t, k}`; las antiguas (solo fecha) se leen igual — la
  transición no reinicia ningún cursor. La comparación de fechas sigue siendo
  por `instante()` (TRAMPAS §1), nunca texto.
- Helpers PUROS extraídos a `apps/nodo/cursores.mjs` + `prueba-cursores.mjs`
  que corre SIN nodo: 2.501 filas con el MISMO updated_at salen exactamente una
  vez con cortes de página en mitad del grupo.

**F8.1 parcial — provisionado paginado** (`provisionar.mjs`): páginas de 1000
por PK hasta agotar; el `limit=5000` silencioso ya no puede instalar un bar
incompleto "con éxito".

**Pendiente F7/F8:** 7.3 (tombstones formales con retención/ack, conflictos
visibles con versión, backoffice local completo — necesita migración
`sync_cursor_ledger_y_tombstones` y diseño de resolución), probar 7.2 contra el
nodo real al levantarlo (`prueba-sync.mjs` + catálogo grande), updater/cola de
impresión (desktop sucio en otra sesión), CI (plans/025).

## 16. Sesión chat (Claude) — F7.3 tombstones + F8.3 CI («sigue con el f7 y f8»)

Verificación: lint **0 errores** (primera vez; 45 warnings de baseline) ·
typecheck 12/12 · tests 91/91 · contrato 691 usos (96 tablas/55 RPC) ·
prueba-cursores verde · humo de tombstone en vivo.

**F7.3 núcleo — 0120 `tombstones_sync` APLICADA por MCP:**
- Cada DELETE de catálogo deja lápida `{tabla, clave jsonb, updated_at}` —
  trigger genérico (lee la PK de `pg_index`) colgado de **60 tablas** con el
  mismo criterio de la 0101; excluidos operativo/fiscal y la fontanería de
  identidad. Humo: alta+baja de un alérgeno deja su lápida con tenant correcto.
- La columna de fecha se llama `updated_at` A PROPÓSITO: los cursores compuestos
  de `cursores.mjs` sirven tal cual para paginar lápidas (marca `tumba:tabla`).
- `sincronizar.mjs`: baja lápidas nuevas y (a) borra la fila local si es más
  vieja o igual que su lápida, (b) el filtro de subida NUNCA sube una fila
  enterrada — **la resurrección por backup antiguo está muerta** —, (c) una
  edición local POSTERIOR a la lápida gana (LWW) y re-sube. Marca al final del
  pase (relectura idempotente si muere a mitad). Compat: nube sin 0120 → fetch
  falla → todo sigue como antes.

**F8.3 — CI con gates de verdad (`.github/workflows/ci.yml`):**
- Baseline honesto: arreglados los 8 errores de lint (5 eran de los scripts
  nuevos: NUL/BOM literales y regex con espacios; 3 preexistentes de variables
  sin usar). `pnpm lint` pasa por primera vez; los 45 warnings solo pueden bajar.
- Gates nuevos en Linux: `contrato:check` (sin red), `prueba-cursores` (pura),
  numeración de migraciones única (dos sesiones con la misma NNNN = fallo).
- **Runner Windows nuevo**: install + contrato (encoding) + cursores +
  typecheck + tests — cubre BOM/paths/runtime real del bar que Ubuntu no ve.
- Falta de 025: harness DB en 55432 dentro de CI (pide contenedor/nodo
  desechable) para cablear las matrices RLS/dinero/identidad como gates.

**Estado del programa tras hoy**: F0 ✔ · F1 núcleo ✔ (contract 1.5 tras canary)
· F2 núcleo ✔ · F3 3.1/3.2 ✔ · F4 núcleo ✔ · F5 núcleo ✔ · F6 núcleo ✔ ·
F7 núcleo ✔ · F8 parcial. Migraciones 0111–0120 TODAS aplicadas en la nube;
**NINGUNA en el nodo** (apagado). Bloqueos externos: puertas 5/6 (licencia,
propina/redondeo), ficheros desktop/tpv en otra sesión, gestión de claves para
la temporal cifrada, nodo desechable para el harness.

## 17. Sesión chat (Claude) — PUSH + rematar lo implementable

**PUSH HECHO**: `4d11ee5..d688d81` en `nodo-local` — commit `91ecfb1`
(programa F0–F8 completo: 107 ficheros, 0111–0120, docs, CI) + commit `d688d81`
(wip del TPV/escritorio de la otra sesión, para que exista en el remoto).
Higiene previa: `CREDENCIALES-PRUEBA.local.md` ignorado ✓, sin secretos en lo
nuevo ✓, `.codex/config.toml` sin credenciales ✓.

**Rematado después del push** (verificación: lint 0 err · typecheck 12/12 ·
tests 91/91 · contrato 691 usos, 97 tablas/59 RPC):

- **0121 `sesion_soporte_y_break_glass` APLICADA** (F5.3): soporte con
  consentimiento del titular (PENDIENTE→APROBADA por un PROPIETARIO del tenant),
  break-glass exige MFA (claim `aal2`) + motivo y AVISA, **máximo 2 horas
  impuesto por CHECK** (humo verde: ni el service role puede saltárselo), sin
  autorrenovación (no existe UPDATE de `hasta`), revocable por ambas partes,
  todo en `evento_seguridad`. `soporte_vigente(tenant)` queda para cablear el
  enforcement cuando llegue la Gestión remota.
- **0122 `identidad_global_contract` ESCRITA, NO APLICADA** (F1.5): retira las
  unicidades globales de `app_user` y crea `unique(tenant, cuenta)` +
  `unique(tenant, lower(email))`. Las CONDICIONES de aplicación van dentro del
  fichero (canary F1 + puerta 8). Las instalaciones NUEVAS del nodo sí la
  aplican desde cero (correcto: nacen con el modelo final).
- **MFA obligatorio para cuentas Gluuh** (F2.3): `mfaPlataformaInsuficiente()`
  en `lib/plataforma.ts` — con `MFA_GLUUH_OBLIGATORIO=1`, las 4 rutas de
  plataforma (crear-empresa, empresa, generar-licencia, orden-instalacion)
  exigen sesión `aal2`. **APAGADO por defecto**: enrolar el TOTP del admin en
  /seguridad ANTES de encender la variable, o se deja fuera a sí mismo.

**Lo que queda del programa** (sin cambios; ver §16 y REPARACION-F0-F8.md):
nodo (aplicar 0111–0121 + pruebas), puertas 5/6, desktop/tpv (otra sesión),
backups cifrados y updater (F8.2), harness DB en CI, F8.4 rendimiento.

## 18. Sesión chat (Claude) — INSTALADORES NUEVOS (nodo + TPV)

El usuario no tiene el nodo instalado (prueba con `pnpm --filter @gluuh/web dev`);
pidió los instaladores nuevos. TRAMPAS §3–§6 leídas antes de tocar.

**Instalador del NODO (`Instalar-Gluuh.ps1`) — flujo de ORDEN (F3/0116):**
- Canje en DOS escalones: 1º la ORDEN via `app.gluuh.com/api/instalacion/activar`
  (un solo uso, ligada a local, registra `nodo_instancia`); 2º fallback legacy
  `empresa_por_codigo` si la web no responde o el código es de la generación
  anterior. 410 caducado y 409 "otro equipo instalando" con mensajes claros.
- **Intento REANUDABLE**: un GUID persistido en `%TEMP%\gluuh-instalacion-intento.txt`
  — reejecutar tras un corte retoma la reserva de 24 h; se borra al terminar.
- **Identidad del nodo**: `NODO_FINGERPRINT` (+ `NODO_ID` si canje por orden) se
  guardan en `.nodo\nodo.env` — es lo que permite revocar ESTE nodo desde el
  panel. (El par de claves criptográfico completo queda para el canje v2.)
- Contador de migraciones DINÁMICO (aplicará 0111–0122 solo; `instalar-nodo.ps1`
  ya recorría la carpeta). Nota: las instalaciones nuevas nacen con el modelo
  contract de 0122 — correcto.
- `prueba-instalador.ps1` VERDE en el PS 5.1 real (11 scripts, sintaxis + BOM).

**UI admin**: botón **"Emitir orden de instalación"** en la ficha de empresa
(`/admin/empresas/[id]`) — enseña el código UNA vez (en BD solo el hash).
Sin esto no había forma humana de probar el flujo nuevo.

**Artefactos compilados:**
- `apps\desktop\dist-instalador\Gluuh TPV Setup 0.1.0.exe` (97 MB, NSIS
  asistido, electron-builder) — el instalador del TPV.
- `C:\gluuh-paquete\dist\GluuhServidor-1.0.0.exe` — recompilado con
  `Montar-Paquete.ps1` (Inno Setup local), con el instalador nuevo y las
  migraciones 0111–0122 dentro. (Ver §19 si terminó bien.)
- Ninguno va FIRMADO todavía (bloqueado viejo de AHORA.md: certificado de firma).

**Cómo probarlo de cero en esta máquina** (no hay clientes reales):
1. En `/admin` → ficha de la empresa de pruebas → «Emitir orden de instalación».
2. ⚠ La `.nodo\` actual es de una instalación anterior: `Instalar-Gluuh.ps1`
   RECREA la base (TRAMPAS §6) — con datos de prueba, adelante.
3. Ejecutar `C:\gluuh-paquete\dist\GluuhServidor-1.0.0.exe` con el código de la
   orden → debe registrar `nodo_instancia` en la nube y quedar CANJEADA.
4. Instalar `Gluuh TPV Setup 0.1.0.exe` en la misma máquina y apuntarlo a la IP.
5. Después: correr las pruebas adversariales contra el nodo levantado.

## 19. Sesión chat (Claude) — instalación real + TPV 1 y titular por defecto

- **El usuario INSTALÓ el nodo con el .exe nuevo** (empresa "Plantilla base",
  http://172.16.5.8:54321). El paquete llevaba las migraciones 0001–0122: el
  nodo nace ya con el modelo nuevo (fail-closed, contract incluido).
- Hueco encontrado en vivo: la plantilla llegó con **0 usuarios y 0 dispositivos**
  → ni panel ni emparejado posibles. Creados a mano en el nodo vivo: TPV 1 con
  código de emparejado (24 h) y Titular `admin` con clave+PIN aleatorios
  (entregados por chat; guardarlos en CREDENCIALES-PRUEBA.local.md).
- **Instalador actualizado (petición del usuario, alineada con plan 14 F3):**
  `Instalar-Gluuh.ps1` crea SIEMPRE "TPV 1" pendiente de emparejar (código de
  6 dígitos, un uso, 24 h, impreso en INSTALACION.txt) y, SOLO si el bar llega
  sin ningún usuario, un Titular `admin` con clave y PIN **aleatorios por
  instalación** impresos una vez en la hoja — nada de credenciales iguales
  para todos (eso murió hoy). Bares con usuarios de la nube: no se toca.
- `prueba-instalador.ps1` verde de nuevo (PS 5.1 + BOM).
- `GluuhServidor-1.0.0.exe` recompilándose con esto dentro.
- El exe de 18:40 quedó obsoleto en ese detalle; el TPV Setup no cambia.
