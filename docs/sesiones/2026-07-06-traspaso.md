# Traspaso de sesión — 06/07/2026

Documento para continuar el trabajo en otro chat. Contexto: rediseño del **catálogo al modelo Glop/Ágora** + endurecimiento de la base de datos. El plan de fases y el mapeo campo a campo están en el Artifact vivo:
https://claude.ai/code/artifact/c7e25a90-8775-4799-855d-7f541ff1e598

Referencia del cliente: manual de Glop (`Downloads/MANUAL COMERCIOS.pdf`, secciones 2.5 / 2.8 / 2.10 / 2.11). El cliente quiere replicar Glop tal cual (familia · categoría · producto), **sin inventar** ejemplos propios.

---

## 1. Modelo decidido (con el cliente)

- **Familia = propiedades heredables** (1 por producto): formatos de venta (caña/tubo/copa), grupos de comentarios a cocina, ingredientes extra (con precio), notas, IVA y estilo.
- **Categoría = agrupación, muchos-a-muchos**: un producto puede estar en N categorías (p. ej. "Cerveza 1925" en CERVEZA y BEBIDAS).
- **Las familias NO se muestran en el TPV** (son configuración): el TPV enseña categorías + productos.
- `mostrar_venta` (bool) en familia y categoría: solo salen en el TPV si está marcado.
- **Biblioteca compartida por tenant** de grupos de extras/comentarios/notas, reutilizable entre familias; se asigna a familia → categoría → producto con **suma + añadir/quitar por nivel**.
- Semilla por defecto al primer arranque: biblioteca base (puntos de carne, sin gluten, extras típicos) + carta de ejemplo.

## 2. Hecho (aplicado y verificado)

### Fase 1 del modelo Glop — migración 0061 (APLICADA en Supabase)
- Tabla `product_category` (m2m) con backfill automático desde `product.category_id` (115 productos enlazados).
- Columnas `mostrar_venta` / `mostrar_menus` en `family` y `category`.
- Ejemplo real de m2m: categoría **"Populares"** en Bar Demo con 9 superventas (cada uno en 2 categorías).

### TPV adaptado (build limpio) — **cambios SIN COMMITEAR en el working tree**
- `apps/web/app/lib/catalogo-store.ts`: el store carga `product_category` y `category.mostrar_venta` **best-effort** (si la migración no existe, degrada sin romper); nuevo `prodCats: Record<productId, categoryId[]>` persistido.
- `apps/web/app/tpv/page.tsx`: `productosCat` filtra por la m2m con fallback a `category_id`; `catsVisibles` oculta categorías con `mostrar_venta === false`; flechas de categorías movidas junto al buscador y buscador más compacto.
- **Primera tarea del próximo chat: revisar y commitear estos dos ficheros** (`git diff` los muestra; ~37 líneas).

### Endurecimiento de BD — migraciones 0062 y 0063 (APLICADAS, ficheros nuevos sin commitear)
- `0062_indices_fk.sql`: 73 FK sin índice → 0 (74 índices creados).
- `0063_hardening_funciones_politicas.sql`: políticas RLS duplicadas en `cash_move`/`cash_session` (2→1) + `search_path` fijo en `set_updated_at`, `setting_get`, `setting_set`.
- Se dejó a propósito: 15 índices "no usados" (BD reciente) y la política de `platform_admin` (0 filas).

### Trabajo previo relevante (04/07, ya commiteado)
- Configuración backoffice completa: zona técnica con clave de 8 caracteres por cliente, `/personalizar` como única página de marca, `/copias-de-seguridad`. Fuente de verdad: `docs/implementacion/11-configuracion-backoffice.md`. Migraciones 0045–0059 (0056 logo de ticket puede quedar por aplicar).
- TPV: 3 modales (Cobrar/Dividir/Modificadores) cableados, pago mixto, velo de bloqueo, atribución de camarero por línea (0059), planos de mesas (`/planos-de-mesas`).
- Carta en páginas separadas estilo Ágora: `/familias`, `/categorias`, `/productos`.

## 3. Pendiente (en orden)

1. ~~**Commitear el working tree**~~ ✔ hecho en la sesión 2 (ver §5): `de37ebc` (TPV m2m), `19e092a` (migraciones 0062/0063), `c4401e2` (reorganización de docs).
2. ~~**Fase 2 — el cambio grande**~~ ✔ COMPLETA (sesión 3, ver §6): código + 0064 aplicada y registrada + semilla sembrada + herencia verificada en vivo (27 productos de Comida). Los formatos heredables desde familia quedan para una Fase 2b (necesitan precio por producto×formato).
3. **Fase 3**: ficha completa de producto (PLU, principal/añadido, tarifas), etiquetas, anidamiento (familia/categoría padre), grupos de trabajo dedicados.
4. **Panel de Supabase (manual, lo hace el usuario)**: activar *leaked password protection* en Auth y revisar el bucket `media` (permite listar archivos).
5. Pendientes menores arrastrados: chips de `nota_preparacion` en el editor de línea del TPV; que `apps/desktop` lea de `setting` en vez de `config.json`; ámbitos LOCAL/DEVICE de `setting` casi sin uso.

## 4. Datos operativos (importante para no tropezar)

- **Supabase**: proyecto "Gluuh - tpv", ref `gxcqihslbicrszgzudjs`. El MCP `mcp__supabase__*` se autorizó por OAuth en sesión interactiva, pero **en sesiones no interactivas puede aparecer sin autenticar** (pasó en la sesión 3): re-autorizar con `/mcp` → supabase → Authenticate y aplicar DDL con `apply_migration` / `execute_sql`. **El historial `supabase_migrations` está VACÍO** (las migraciones antiguas se aplicaron por editor SQL); las nuevas se aplican por MCP **y** se versionan como fichero en `supabase/migrations/`.
- Tenants reales: **Bar Demo** (`11111111-…`, Canarias/IGIC, con demo de variaciones/modificadores/notas) y **Gluuh plataforma** (`ca44a7c7-…`). El tenant del seed `supabase/seed-ejemplo-carta.sql` (328063c3) NO existe en esta BD.
- Gotcha: `product_format` por REST exige `tenant_id` explícito (el trigger `set_tenant_id` lanza "Sin tenant").
- Esquema relevante: `category.family_id` (1→1), `product.category_id` (1→1, ahora conviviendo con la m2m), `product_format`, `modifier_group`/`modifier` (por producto y, con la 0064, también biblioteca con `modifier_group_asignacion`), `nota_preparacion`, `product.pase`/`estacion`.
- Leer antes de tocar `supabase/`: skill `.agents/skills/gluuh-base-datos`. Para el TPV: `gluuh-tpv-glop` y `gluuh-ux-operativa`.

---

## 5. Sesión 2 del 06/07 — reorganización de docs y commits

- **`docs/` reorganizado por completo** (commit `c4401e2`): raíz solo con el README índice; carpetas `plan/` (antes `auditoria_02_07_26`), `referencia/` (antes `auditoria`, con `diseno/` en vez del segundo `implementacion/`), `dossier/` (01–15), `especificaciones/` y `sesiones/`. Eliminados por duplicados: el PDF de Ágora (23 MB, ya destilado), el doc 16 (absorbido por `especificaciones/paginas-creacion-configuracion.md`) y el 19 (su patrón de página vive en la guía de diseño §19). La guía de diseño se adaptó de Senzu a Gluuh. Verificado: 0 enlaces rotos en los 122 markdown.
- **Skill nueva `.agents/skills/gluuh-registro`**: plantilla de traspaso por sesión en `docs/sesiones/` y qué documentos vivos actualizar al cerrar.
- **Commiteado el trabajo de la sesión 1**: `de37ebc` (TPV: m2m + `mostrar_venta`, diff revisado) y `19e092a` (ficheros de migraciones 0062/0063, ya aplicadas en Supabase).
- Rutas actualizadas en CLAUDE.md y skills: el checklist maestro ahora es `docs/plan/08-checklist-maestro-100.md`; el modelo de datos objetivo, `docs/referencia/diseno/modelo-de-datos.md`.

---

## 6. Sesión 3 del 06/07 — Fase 2 del modelo Glop (código + BD APLICADA)

**Biblioteca de modificadores con herencia familia → categoría → producto**, de punta a punta. La BD se aplicó al final de la sesión, cuando el usuario conectó el MCP con **token personal** (`claude mcp add --scope local supabase -e SUPABASE_ACCESS_TOKEN=… -- npx -y @supabase/mcp-server-supabase@latest --project-ref=…`) — el OAuth interactivo no le funcionaba; esta es la vía buena para sesiones no interactivas.

### Hecho
- **Migración `0064_biblioteca_modificadores.sql` (SIN APLICAR)**: `modifier_group.product_id` pasa a opcional (NULL = biblioteca), columna `tipo` EXTRA/COMENTARIO con backfill por precios, tabla `modifier_group_asignacion` (destino XOR familia/categoría/producto, `modo` INCLUIR/EXCLUIR, únicos parciales por nivel, RLS + trigger tenant + índices FK).
- **Semilla** `supabase/seed-biblioteca-modificadores.sql` (punto de la carne, alergias, extras típicos) para Bar Demo, idempotente. TODO: cablearla al alta de empresa.
- **Espejo `apps/api/db/schema.sql`** actualizado — incluye también la **0061 que faltaba** (product_category, mostrar_venta).
- **Store** (`catalogo-store.ts`): carga biblioteca + asignaciones best-effort y expone `gruposDeProducto()` — resolver puro de herencia (familia → categorías m2m → producto; en cada nivel EXCLUIR primero, INCLUIR gana). **Verificado con 7 escenarios** (script one-off, la web no tiene Vitest montado).
- **TPV** (`tpv/page.tsx`): el modal de modificadores consume los grupos efectivos (propios + heredados); split comentarios/extras por `tipo` con fallback al heurístico de precio de siempre.
- **Panel**: página nueva `/modificadores` (CRUD de biblioteca con conteo de asignaciones), componente `components/asignaciones-biblioteca.tsx` integrado en fichas de **familia** (switch Incluir), **categoría** y **producto** (Heredar/Incluir/Quitar + badge «Heredado»), entrada en el nav (Administración → Catálogo).
- Todo degrada con aviso ámbar si la 0064 no está aplicada (patrón 0058/0061). Typecheck limpio.

### Aplicado en Supabase (mismo día, por MCP)
- El usuario había pegado la 0064 en el SQL Editor (esquema ya presente pero fuera del historial): se **re-aplicó con `apply_migration`** (idempotente) y quedó **registrada** como `biblioteca_modificadores` junto a 0061–0063.
- **Semilla sembrada** en Bar Demo Gluuh: 3 grupos de biblioteca (Punto de la carne, Alergias e intolerancias, Extras; 13 opciones). Gotcha corregido en el fichero: buscaba `ilike 'bar demo'` exacto y el tenant se llama **"Bar Demo Gluuh"** → ahora `%bar demo%` (por eso la ejecución manual del usuario no sembró nada).
- Backfill de `tipo` verificado sobre los 23 grupos por producto: 10 COMENTARIO / 13 EXTRA.
- **Demo de herencia en vivo**: «Punto de la carne» y «Extras» incluidos en la familia **Comida** → **27 productos** los heredan (verificado con el equivalente SQL del resolver).
- `get_advisors` (security): **ningún aviso nuevo** por la 0064; siguen los conocidos (leaked password protection y bucket `media` = tarea manual del usuario, pendiente nº 4).

### Primera tarea del próximo chat
1. Verlo en la interfaz: `/modificadores` en el panel y el modal del TPV con un bocadillo de la familia Comida (debe ofrecer Punto de la carne + Extras). Los avisos ámbar ya no deben salir.
2. Seguir con la **Fase 3** (ficha completa de producto) o cablear la semilla al alta de empresa.

---

## 7. Sesión 4 del 06/07 — catálogo estilo Ágora (capturas del cliente)

El cliente pasó capturas de su Ágora real (lapalma) y pidió replicar la disposición. Hecho de punta a punta (BD por MCP + panel):

- **Migración `0065_ficha_glop.sql` (APLICADA y registrada)**: `familia_padre_id` + `orden_impresion` + `texto_boton` + `foto_url` en familia; `categoria_padre_id` + `texto_boton` + `carta_nombre/carta_descripcion` en categoría; **`product.family_id` (familia DIRECTA, backfill 109/115)** + `plu` (único por tenant) + `es_principal`/`es_anadido` + `tiempo_preparacion_min` + `texto_boton` + `carta_nombre` en producto; tabla `category_sales_center` (sin filas = todos los centros). Espejo al día.
- **Resolver de herencia**: prefiere `product.family_id`; cae a la derivada de la categoría principal.
- **Panel a ancho completo, disposición Ágora** (piel Supabase, regla de la guía de diseño):
  - Listados con tabla densa + búsqueda + «N registros» + acciones al pasar el ratón: **familias** (grupo mayor, padre, orden imp., Sí/No de TPV/menús), **categorías** (familia, padre, Sí/No), **productos** (familia, categorías m2m, PLU, impuesto, tipo prep., principal/añadido, precio, estado; scroll horizontal), **grupos mayores**.
  - Fichas en dos paneles (datos + Estilo) con Aceptar/Cancelar/Eliminar: familia (padre, grupo mayor, orden impresión, texto/color/imagen, checks de venta/menús, **productos de la familia** con buscar-y-añadir), grupo mayor (**familias del grupo**), categoría (padre, familia, estación, estilo, **productos m2m**, **centros de venta** con radio asociar-a-todos, **carta digital**), producto (familia directa, PLU, tiempos, principal/añadido, estilo de impresión, **categorías m2m con principal ★**, carta digital; conserva formatos/añadidos/copiar/alérgenos/biblioteca).
  - Componente compartido `components/buscar-anadir.tsx` (patrón «Buscar (F3) y añadir»).
- Typecheck limpio. Nota: el editor de categoría ya no tiene el botón «aplicar estación a los N productos» (poco uso; recuperable de git si se echa en falta).

### Pendiente Fase 3b (para completar la paridad de las capturas)
- Grupos de puntos de venta (agrupación de dispositivos TPV) — no existe el concepto aún.
- Horario por categoría, precios por centro/tarifa en la ficha, etiquetas de producto (tabla `tag`), almacén/compras en ficha de producto, imagen en carta digital de categoría.
- TPV: usar `texto_boton`/`foto_url` de familia y `mostrar_venta` de familia (hoy solo se usa el de categoría).
