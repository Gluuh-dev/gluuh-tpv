{
  "name": "servio-tpv",
  "version": "0.0.0",
  "private": true,
  "description": "Plataforma TPV de hostelería (web + Windows + Android/iOS + backend). Monorepo.",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "core:demo": "pnpm --filter @servio/core demo",
    "core:test": "pnpm --filter @servio/core test"
  },
  "devDependencies": {
    "turbo": "^2.1.0",
    "typescript": "^5.6.0"
  }
}
